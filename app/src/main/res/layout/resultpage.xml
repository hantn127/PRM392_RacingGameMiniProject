<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:id="@+id/resultPage"
    android:background="@drawable/bg_water">

    <!-- Tiêu đề kết quả -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🏆 Race Results 🏆"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FFD700"
        android:fontFamily="cursive"
        android:layout_marginBottom="16dp"/>

    <!-- Ảnh minh họa -->
    <ImageView
        android:id="@+id/ivWinner"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/vit2"
        android:layout_marginBottom="12dp"/>

    <!-- Vịt thắng cuộc -->
    <TextView
        android:id="@+id/tvWinningDuck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🏁 Winning Duck: #1"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="8dp"/>

    <!-- Số tiền thắng -->
    <TextView
        android:id="@+id/tvAmountWon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="💰 You Won: $500"
        android:textSize="20sp"
        android:textColor="#00FF00"
        android:layout_marginBottom="8dp"/>

    <!-- Số tiền thua -->
    <TextView
        android:id="@+id/tvAmountLost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="💵 You Lost: $100"
        android:textSize="20sp"
        android:textColor="#FF0000"
        android:layout_marginBottom="8dp"/>

    <!-- Số dư hiện tại -->
    <TextView
        android:id="@+id/tvFinalBalance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🏦 Remaining Balance: $1400"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#FFD700"
        android:layout_marginBottom="16dp"/>

    <!-- Nút xác nhận -->
    <Button
        android:id="@+id/btnConfirm"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:background="@drawable/button_duck"
        android:text="OK ✅"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:padding="12dp"/>

</LinearLayout>
